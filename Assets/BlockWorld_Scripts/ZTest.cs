using UnityEngine;

public class ZTest : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        transform.localScale = new Vector3(50, 200, 50);
    }

    // Update is called once per frame
    void Update()
    {
        transform.localScale = new Vector3(50, 200, 50);
    }
}
